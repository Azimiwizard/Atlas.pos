# Changelog

## Unreleased

- Deprecated `category_id` in Backoffice product APIs – it is now a compatibility alias for `menu_category_id`. Please migrate clients to `menu_category_id`; the legacy field will be removed once all consumers switch over.

